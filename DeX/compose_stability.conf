// Compose compiler stability configuration.
// Each line is a fully-qualified class the compiler treats as @Stable.
// These types come from the backdrop library and contain only stable
// (immutable-after-construction) properties, but the compiler cannot
// prove it because the types live in an external module.

com.kyant.backdrop.highlight.Highlight
com.kyant.backdrop.shadow.Shadow
com.kyant.backdrop.shadow.InnerShadow
