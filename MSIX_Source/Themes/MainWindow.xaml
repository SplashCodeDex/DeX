<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Name="winSpatial"
        WindowStyle="None" Background="Transparent" AllowsTransparency="True"
        Topmost="True" ShowInTaskbar="False"
        Width="1420" Height="760"
        ResizeMode="NoResize">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="AppStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        <!-- Shared card shadow: a resource so the engine can suspend/re-apply it during resize transitions. -->
        <DropShadowEffect x:Key="MainShadow" BlurRadius="14" ShadowDepth="8" Direction="270" Color="Black" Opacity="0.5" />
        <ContextMenu x:Key="TransferContextMenu" Background="{DynamicResource PrimaryBrush}" Foreground="{DynamicResource PrimaryTextBrush}" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1" Padding="4">
            <ContextMenu.Resources>
                <Style TargetType="MenuItem">
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Padding" Value="10,6" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="MenuItem">
                                <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}" Margin="0,2">
                                    <ContentPresenter ContentSource="Header" RecognizesAccessKey="True" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsHighlighted" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SecondaryHoverBrush}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ContextMenu.Resources>
            <MenuItem Header="Open" Name="CtxOpen" />
            <MenuItem Header="Open Containing Folder" Name="CtxOpenFolder" />
            <MenuItem Header="Copy Path" Name="CtxCopyPath" />
            <Separator Background="{DynamicResource AccentBrush}" Margin="0,4" />
            <MenuItem Header="Delete" Name="CtxDelete" Foreground="#FF6B6B" />
        </ContextMenu>
        </ResourceDictionary>
    
</Window.Resources>
    <Border x:Name="mainBorder" HorizontalAlignment="Right" VerticalAlignment="Bottom" CornerRadius="34" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1" Background="{DynamicResource PrimaryBrush}" RenderTransformOrigin="0.5,1" Margin="25" Effect="{StaticResource MainShadow}">
        <Border.RenderTransform>
            <TransformGroup>
                <ScaleTransform ScaleX="1" ScaleY="1" x:Name="winScale" />
                <TranslateTransform Y="0" x:Name="winTrans" />
            </TransformGroup>
        </Border.RenderTransform>

        <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        
        <!-- File Explorer Panel (left side, hidden by default) -->
        <Grid Name="FileExplorer" Grid.Column="0" Visibility="Collapsed" Opacity="0" Margin="24,24,15,15" AllowDrop="True" Background="Transparent">
            <Grid.RenderTransform>
                <TranslateTransform x:Name="fileTrans" X="150" />
            </Grid.RenderTransform>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <Grid Grid.Row="0" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <Button Name="btnUpDir" Grid.Column="0" Background="{DynamicResource AccentBrush}" BorderThickness="0" Foreground="{DynamicResource PrimaryTextBrush}" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="16" Content="&#xE898;" Cursor="Hand" ToolTip="Up Directory" Margin="0,0,12,0" VerticalAlignment="Center" Padding="12" Visibility="Collapsed">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="18">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                
                <Border Grid.Column="1" Background="{DynamicResource AccentBrush}" CornerRadius="20" Padding="14,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="&#xE721;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="14" Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <TextBox Name="txtSearch" Grid.Column="1" Text="Search transfers..." FontSize="14" Foreground="{DynamicResource SecondaryTextBrush}" CaretBrush="{DynamicResource PrimaryTextBrush}" Background="Transparent" BorderThickness="0" FontWeight="SemiBold" VerticalAlignment="Center" FontFamily="Segoe UI" Padding="0" Margin="0" />
                    </Grid>
                </Border>
                
                <Button Name="btnToggleExplorerMode" Grid.Column="2" Background="{DynamicResource AccentBrush}" BorderThickness="0" Foreground="{DynamicResource PrimaryTextBrush}" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="16" Content="&#xE8B7;" Cursor="Hand" ToolTip="Toggle File Explorer Mode" Margin="12,0,0,0" VerticalAlignment="Center" Padding="12">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="18">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button Name="btnProfileTop" Grid.Column="3" Style="{StaticResource SpatialListItem}" Margin="12,0,12,0" ToolTip="Settings" VerticalAlignment="Center" Padding="0" Visibility="Collapsed" Opacity="0">
                    <Grid>
                        <Ellipse Width="34" Height="34">
                            <Ellipse.Fill>
                                <ImageBrush ImageSource="file:///$($PSScriptRoot -replace '\\', '/')/../Assets/ProfileAvatar.jpg" Stretch="UniformToFill" AlignmentY="Top" />
                            </Ellipse.Fill>
                        </Ellipse>
                    </Grid>
                </Button>
            </Grid>
            
            <ListBox Name="lbFiles" Grid.Row="1" SelectionMode="Extended" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Hidden" Padding="0,0,10,0">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="Padding" Value="2"/>
                        <Setter Property="Margin" Value="2"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border Name="itemBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10" SnapsToDevicePixels="True">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="itemBorder" Property="Background" Value="{DynamicResource SecondaryHoverBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="itemBorder" Property="Background" Value="{DynamicResource SecondarySelectedBrush}"/>
                                            <Setter TargetName="itemBorder" Property="BorderBrush" Value="{DynamicResource SecondarySelectedBorderBrush}"/>
                                        </Trigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <Setter TargetName="itemBorder" Property="Background" Value="{DynamicResource SecondarySelectedHoverBrush}"/>
                                            <Setter TargetName="itemBorder" Property="BorderBrush" Value="{DynamicResource SecondarySelectedBorderBrush}"/>
                                        </MultiTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel IsItemsHost="True" Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
            
            <!-- Empty Folder State Overlay -->
            <StackPanel Name="emptyFolderState" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed" Opacity="0">
                <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="&#xE8B7;" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="48" HorizontalAlignment="Center" Opacity="0.4" Margin="0,0,0,10"/>
                <TextBlock x:Name="txtEmptyStateTitle" Text="No transfers yet" FontSize="14" Foreground="{DynamicResource SecondaryTextBrush}" FontWeight="Medium" HorizontalAlignment="Center" Opacity="0.6"/>
                <TextBlock x:Name="txtEmptyStateSub" Text="Received files appear here" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" HorizontalAlignment="Center" Opacity="0.5" Margin="0,2,0,0"/>
            </StackPanel>
            
            <StackPanel Grid.Row="2" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,15,0,25">
                <Separator Background="{DynamicResource AccentBrush}" Height="1" Width="300" Margin="0,0,0,20" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <ToggleButton Name="btnPushFiles" Style="{DynamicResource PushBtnStyle}" Margin="0,0,30,0" Cursor="Hand">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="&#xE8E5;" FontSize="19" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock Text="Send Files" FontSize="13" VerticalAlignment="Center" FontWeight="Medium"/>
                        </StackPanel>
                    </ToggleButton>
                    <ToggleButton Name="btnPushFolder" Style="{DynamicResource PushBtnStyle}" Cursor="Hand">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="&#xE8B7;" FontSize="19" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock Text="Send Folders" FontSize="13" VerticalAlignment="Center" FontWeight="Medium"/>
                        </StackPanel>
                    </ToggleButton>
                </StackPanel>
            </StackPanel>
            
            <!-- Subtle Floating Dock for Download Status & Location Change -->
            <Border Name="dockDownloadToast" Grid.Row="2" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,15" Background="{DynamicResource AccentBrush}" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1" CornerRadius="20" Padding="14,8" Visibility="Collapsed" Opacity="0" RenderTransformOrigin="0.5,1">
                <Border.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform x:Name="dockScale" ScaleX="0.8" ScaleY="0.8" />
                        <TranslateTransform x:Name="dockTrans" Y="25" />
                    </TransformGroup>
                </Border.RenderTransform>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="&#xE8F4;" Foreground="{DynamicResource SecondaryBrush}" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <TextBlock Name="txtDownloadToast" Text="Saved to Downloads\DeX" FontSize="13" Foreground="{DynamicResource PrimaryTextBrush}" FontWeight="Medium" VerticalAlignment="Center" Margin="0,0,12,0" />
                    <Button Name="btnChangeDownloadPath" Content="Change" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryBrush}" Background="Transparent" BorderThickness="0" Cursor="Hand" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Pull Progress Dock (PC File Explorer: pulling files off the phone) -->
            <Border Name="dockPullProgress" Grid.Row="2" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,55" Background="{DynamicResource AccentBrush}" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1" CornerRadius="16" Padding="14,10" Visibility="Collapsed" Opacity="0" Width="360" RenderTransformOrigin="0.5,1">
                <Border.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform x:Name="pullDockScale" ScaleX="0.85" ScaleY="0.85" />
                        <TranslateTransform x:Name="pullDockTrans" Y="25" />
                    </TransformGroup>
                </Border.RenderTransform>
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Name="txtPullTitle" Text="Pulling files from phone..." FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <Button Name="btnCancelPull" Content="&#xE711;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Background="Transparent" BorderThickness="0" Cursor="Hand" VerticalAlignment="Center"/>
                    </StackPanel>
                    <ProgressBar Name="prgPullProgress" Height="4" Minimum="0" Maximum="100" Value="0" Foreground="{DynamicResource SecondaryBrush}" Background="{DynamicResource PrimaryBrush}" BorderThickness="0" Margin="0,8,0,0" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- Settings Panel (left side, hidden by default, toggled by avatar click) -->
        <Grid Name="SettingsPanel" Grid.Column="0" Visibility="Collapsed" Opacity="0" Margin="24,24,15,15">
            <Grid.RenderTransform>
                <TranslateTransform x:Name="settingsTrans" X="150" />
            </Grid.RenderTransform>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Settings Header -->
            <Grid Grid.Row="0" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <Border Grid.Column="0" Background="{DynamicResource AccentBrush}" CornerRadius="18" Padding="12,8" Margin="0,0,12,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE713;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="16" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <TextBlock Text="Settings" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center" />
                    </StackPanel>
                </Border>
                
                <Border Grid.Column="1" Background="{DynamicResource AccentBrush}" CornerRadius="20" Padding="14,8">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE946;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="14" Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <TextBlock Name="txtSettingsSubtitle" Text="DeX" FontSize="13" Foreground="{DynamicResource SecondaryTextBrush}" FontWeight="Medium" VerticalAlignment="Center" />
                    </StackPanel>
                </Border>
                
                <Button Name="btnProfileTopSettings" Grid.Column="2" Style="{StaticResource SpatialListItem}" Margin="12,0,0,0" ToolTip="Settings" VerticalAlignment="Center" Padding="0" Visibility="Collapsed" Opacity="0">
                    <Grid>
                        <Ellipse Width="34" Height="34">
                            <Ellipse.Fill>
                                <ImageBrush ImageSource="file:///$($PSScriptRoot -replace '\\', '/')/../Assets/ProfileAvatar.jpg" Stretch="UniformToFill" AlignmentY="Top" />
                            </Ellipse.Fill>
                        </Ellipse>
                    </Grid>
                </Button>
            </Grid>
            
            <!-- Settings Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" PanningMode="VerticalOnly" Margin="0,0,10,0">
                <StackPanel>
                    <!-- Profile Section -->
                    <Border Background="{DynamicResource AccentBrush}" CornerRadius="16" Padding="20" Margin="0,0,0,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Ellipse x:Name="imgProfileAvatar" Width="56" Height="56" Margin="0,0,16,0">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="file:///$($PSScriptRoot -replace '\\', '/')/../Assets/ProfileAvatar.jpg" Stretch="UniformToFill" AlignmentY="Top" />
                                </Ellipse.Fill>
                            </Ellipse>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock x:Name="txtProfileName" Text="DeXStudios" FontSize="18" FontFamily="Segoe UI" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                <TextBlock x:Name="txtProfileEmail" Text="dexify@dex.net" FontSize="13" Foreground="{DynamicResource SecondaryTextBrush}" />
                                <TextBlock Text="Premium User" FontSize="12" Foreground="{DynamicResource SecondaryBrush}" FontWeight="SemiBold" Margin="0,4,0,0" />
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Button x:Name="btnSettingsSignOut" Visibility="Collapsed" HorizontalAlignment="Center" Margin="0,-6,0,10" Padding="14,6" Background="{DynamicResource AccentBrush}" BorderThickness="0" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="12" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <TextBlock Text="Sign out" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryForegroundBrush}" />
                    </Button>

                    <!-- Connection Settings -->
                    <TextBlock Text="Connection" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="4,8,0,8" />
                    
                    <Border Background="{DynamicResource AccentBrush}" CornerRadius="16" Padding="4" Margin="0,0,0,12">
                        <StackPanel>
                            <Button Name="btnSettingsAutoConnect" Style="{StaticResource SpatialListItem}" Margin="0,2">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="&#xE895;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="18" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center" Margin="0,0,14,0" />
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="Auto-Connect ADB" FontSize="14" Foreground="{DynamicResource PrimaryTextBrush}" />
                                        <TextBlock Text="Auto-connect when joining phone hotspot" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" />
                                    </StackPanel>
                                    <Border Name="badgeAutoConnect" Grid.Column="2" Background="{DynamicResource SecondaryBrush}" CornerRadius="10" Padding="10,4" Margin="0,0,4,0">
                                        <TextBlock Name="txtBadgeAutoConnect" Text="ON" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource SecondaryForegroundBrush}" />
                                    </Border>
                                </Grid>
                            </Button>
                            
                            <Button Name="btnSettingsConnectNow" Style="{StaticResource SpatialListItem}" Margin="0,2">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="&#xE71B;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="18" Foreground="{DynamicResource SecondaryBrush}" VerticalAlignment="Center" Margin="0,0,14,0" />
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="Connect ADB Now" FontSize="14" Foreground="{DynamicResource PrimaryTextBrush}" />
                                        <TextBlock Text="Manually trigger ADB connection" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" />
                                    </StackPanel>
                                </Grid>
                            </Button>
                            

                            
                            <Button Name="btnSettingsDnd" Style="{StaticResource SpatialListItem}" Margin="0,2">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="&#xE711;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="18" Foreground="{DynamicResource DangerBrush}" VerticalAlignment="Center" Margin="0,0,14,0" />
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="Do Not Disturb" FontSize="14" Foreground="{DynamicResource PrimaryTextBrush}" />
                                        <TextBlock Text="Auto-reject all pairing/transfer requests" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" />
                                    </StackPanel>
                                    <Border Name="badgeDnd" Grid.Column="2" Background="{DynamicResource AccentBrush}" CornerRadius="10" Padding="10,4" Margin="0,0,4,0">
                                        <TextBlock Name="txtBadgeDnd" Text="OFF" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource SecondaryTextBrush}" />
                                    </Border>
                                </Grid>
                            </Button>
                        </StackPanel>
                    </Border>

                    <!-- Identity -->
                    <TextBlock Text="Identity" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="4,8,0,8" />
                    
                    <Border Background="{DynamicResource AccentBrush}" CornerRadius="16" Padding="4" Margin="0,0,0,12">
                        <StackPanel>
                            <Button Name="btnSettingsGoogleSignIn" Style="{StaticResource SpatialListItem}" Margin="0,2">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="&#xE8C7;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="18" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center" Margin="0,0,14,0" />
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="Sign in with Google" FontSize="14" Foreground="{DynamicResource PrimaryTextBrush}" />
                                        <TextBlock Name="txtSettingsGoogleState" Text="Trust all devices signed in with your email" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" />
                                    </StackPanel>
                                    <TextBlock Grid.Column="2" Text="&#xE76B;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="16" Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center" Margin="0,0,8,0" />
                                </Grid>
                            </Button>
                        </StackPanel>
                    </Border>

                    <!-- Appearance -->
                    <TextBlock Text="Appearance" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="4,8,0,8" />
                    
                    <Border Background="{DynamicResource AccentBrush}" CornerRadius="16" Padding="4" Margin="0,0,0,12">
                        <StackPanel>
                            <Button Name="btnSettingsTheme" Style="{StaticResource SpatialListItem}" Margin="0,2">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="&#xE790;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="18" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center" Margin="0,0,14,0" />
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="Theme" FontSize="14" Foreground="{DynamicResource PrimaryTextBrush}" />
                                        <TextBlock Name="txtSettingsTheme" Text="Dark" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" />
                                    </StackPanel>
                                    <TextBlock Grid.Column="2" Text="&#xE76B;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="16" Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center" Margin="0,0,8,0" />
                                </Grid>
                            </Button>
                        </StackPanel>
                    </Border>

                    <!-- Interaction -->
                    <TextBlock Text="Interaction" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="4,8,0,8" />
                    
                    <Border Background="{DynamicResource AccentBrush}" CornerRadius="16" Padding="4" Margin="0,0,0,12">
                        <StackPanel>
                            <Button Name="btnSettingsWiggleToggle" Style="{StaticResource SpatialListItem}" Margin="0,2">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="&#xE92C;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="18" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center" Margin="0,0,14,0" />
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="Wiggle-to-Open Menu" FontSize="14" Foreground="{DynamicResource PrimaryTextBrush}" />
                                        <TextBlock Name="txtSettingsWiggleToggle" Text="Enabled" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" />
                                    </StackPanel>
                                    <TextBlock Grid.Column="2" Text="&#xE76B;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="16" Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center" Margin="0,0,8,0" />
                                </Grid>
                            </Button>
                        </StackPanel>
                    </Border>

                    <!-- Storage -->
                    <TextBlock Text="Storage" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="4,8,0,8" />
                    
                    <Border Background="{DynamicResource AccentBrush}" CornerRadius="16" Padding="4" Margin="0,0,0,12">
                        <StackPanel>
                            <Button Name="btnSettingsDownloadPath" Style="{StaticResource SpatialListItem}" Margin="0,2">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="&#xE8F4;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="18" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center" Margin="0,0,14,0" />
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="Download Location" FontSize="14" Foreground="{DynamicResource PrimaryTextBrush}" />
                                        <TextBlock Name="txtSettingsDownloadPath" Text="Downloads\DeX" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" />
                                    </StackPanel>
                                    <TextBlock Grid.Column="2" Text="&#xE76B;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="16" Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center" Margin="0,0,8,0" />
                                </Grid>
                            </Button>
                        </StackPanel>
                    </Border>

                    <!-- About -->
                    <TextBlock Text="About" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="4,8,0,8" />
                    
                    <Border Background="{DynamicResource AccentBrush}" CornerRadius="16" Padding="4" Margin="0,0,0,12">
                        <StackPanel>
                            <Button Name="btnSettingsAbout" Style="{StaticResource SpatialListItem}" Margin="0,2">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="&#xE946;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="18" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center" Margin="0,0,14,0" />
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="DeX" FontSize="14" Foreground="{DynamicResource PrimaryTextBrush}" />
                                        <TextBlock Text="Version 1.0.0" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" />
                                    </StackPanel>
                                </Grid>
                            </Button>
                            <Button Name="btnSettingsResetIdentity" Style="{StaticResource SpatialListItem}" Margin="0,2">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="&#xE106;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="18" Foreground="#FF4444" VerticalAlignment="Center" Margin="0,0,14,0" />
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="Reset Identity &amp; Trust" FontSize="14" Foreground="#FF4444" />
                                        <TextBlock Text="Revokes all devices and restarts" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" />
                                    </StackPanel>
                                </Grid>
                            </Button>
                        </StackPanel>
                    </Border>

                    <!-- Bottom spacer for scroll -->
                    <Rectangle Height="20" Fill="Transparent" />
                </StackPanel>
            </ScrollViewer>
        </Grid>
        
        <Border Grid.Column="1" Background="Transparent" Padding="0" MinWidth="300" HorizontalAlignment="Right">
            <Border.RenderTransform>
                <TranslateTransform x:Name="menuTrans" X="0" Y="0" />
            </Border.RenderTransform>
            <Border Background="Transparent" CornerRadius="34">
                <DockPanel Margin="8,12" LastChildFill="True">
                

                <!-- Exit Engine & Profile: docked at bottom so it never moves -->
                <StackPanel DockPanel.Dock="Bottom">
                    <Separator Background="{DynamicResource AccentBrush}" Height="1" Margin="16,8" />
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        
                        <Button Name="btnProfileBottom" Grid.Column="0" Style="{StaticResource SpatialListItem}" Margin="16,0,4,0" ToolTip="Settings" VerticalAlignment="Center" Padding="0" RenderTransformOrigin="0.5,0.5">
                            <Button.RenderTransform>
                                <ScaleTransform x:Name="btnProfileBottomScale" ScaleX="1" ScaleY="1" />
                            </Button.RenderTransform>
                            <Grid>
                                <Ellipse Width="34" Height="34">
                                    <Ellipse.Fill>
                                        <ImageBrush ImageSource="file:///$($PSScriptRoot -replace '\\', '/')/../Assets/ProfileAvatar.jpg" Stretch="UniformToFill" AlignmentY="Top" />
                                    </Ellipse.Fill>
                                </Ellipse>
                            </Grid>
                        </Button>
                        
                        <Button Name="btnExit" Grid.Column="1" Style="{StaticResource SpatialListItem}">
                            <Grid>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                    <TextBlock Text="&#xE7E8;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="16" Foreground="{DynamicResource DangerBrush}" VerticalAlignment="Center" Margin="0,0,8,0" />
                                    <TextBlock Name="txtExitBtn" Text="Exit Engine" FontSize="15" FontFamily="Segoe UI" FontWeight="Medium" Foreground="{DynamicResource DangerBrush}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="&#x2318;Q" FontSize="14" Foreground="{DynamicResource DangerBrush}" HorizontalAlignment="Right" FontFamily="Consolas" VerticalAlignment="Center"/>
                            </Grid>
                        </Button>
                    </Grid>
                </StackPanel>
                
                <!-- Main content holding all views -->
                  <Grid x:Name="menuViewsContainer" ClipToBounds="True">
                        <Grid.Resources>
                            <Storyboard x:Key="SlideInPinAnim">
                                <DoubleAnimation Storyboard.TargetName="menuContentTrans" Storyboard.TargetProperty="X" To="-300" Duration="0:0:0.25"><DoubleAnimation.EasingFunction><PowerEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction></DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetName="menuContentPanel" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2" />
                                <DoubleAnimation Storyboard.TargetName="pinViewTrans" Storyboard.TargetProperty="X" To="0" Duration="0:0:0.3"><DoubleAnimation.EasingFunction><PowerEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction></DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetName="pinViewPanel" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.25" />
                                <DoubleAnimation Storyboard.TargetName="pinContentScale" Storyboard.TargetProperty="ScaleX" From="0.95" To="1" Duration="0:0:0.25"><DoubleAnimation.EasingFunction><PowerEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction></DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetName="pinContentScale" Storyboard.TargetProperty="ScaleY" From="0.95" To="1" Duration="0:0:0.25"><DoubleAnimation.EasingFunction><PowerEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction></DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetName="qrContentScale" Storyboard.TargetProperty="ScaleX" From="0.95" To="1" Duration="0:0:0.25"><DoubleAnimation.EasingFunction><PowerEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction></DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetName="qrContentScale" Storyboard.TargetProperty="ScaleY" From="0.95" To="1" Duration="0:0:0.25"><DoubleAnimation.EasingFunction><PowerEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction></DoubleAnimation>
                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="pinViewPanel" Storyboard.TargetProperty="Visibility"><DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Visible}" /></ObjectAnimationUsingKeyFrames>
                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="menuContentPanel" Storyboard.TargetProperty="Visibility"><DiscreteObjectKeyFrame KeyTime="0:0:0.3" Value="{x:Static Visibility.Hidden}" /></ObjectAnimationUsingKeyFrames>
                            </Storyboard>
                            <Storyboard x:Key="SlideOutPinAnim">
                                <DoubleAnimation Storyboard.TargetName="pinViewTrans" Storyboard.TargetProperty="X" To="300" Duration="0:0:0.25"><DoubleAnimation.EasingFunction><PowerEase EasingMode="EaseIn"/></DoubleAnimation.EasingFunction></DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetName="pinViewPanel" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2" />
                                <DoubleAnimation Storyboard.TargetName="menuContentTrans" Storyboard.TargetProperty="X" To="0" Duration="0:0:0.3"><DoubleAnimation.EasingFunction><PowerEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction></DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetName="menuContentPanel" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.25" />
                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="menuContentPanel" Storyboard.TargetProperty="Visibility"><DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Visible}" /></ObjectAnimationUsingKeyFrames>
                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="pinViewPanel" Storyboard.TargetProperty="Visibility"><DiscreteObjectKeyFrame KeyTime="0:0:0.3" Value="{x:Static Visibility.Collapsed}" /></ObjectAnimationUsingKeyFrames>
                            </Storyboard>
                          <Storyboard x:Key="SwitchQrToPinAnim">
                              <DoubleAnimation Storyboard.TargetName="qrContentTrans" Storyboard.TargetProperty="X" To="-140" Duration="0:0:0.25"><DoubleAnimation.EasingFunction><PowerEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction></DoubleAnimation>
                              <DoubleAnimation Storyboard.TargetName="qrCodeContent" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2" />
                              <DoubleAnimation Storyboard.TargetName="pinContentTrans" Storyboard.TargetProperty="X" To="0" Duration="0:0:0.25"><DoubleAnimation.EasingFunction><PowerEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction></DoubleAnimation>
                              <DoubleAnimation Storyboard.TargetName="pinCodeContent" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.25" />
                              <DoubleAnimation Storyboard.TargetName="pinContentScale" Storyboard.TargetProperty="ScaleX" From="0.95" To="1" Duration="0:0:0.25"><DoubleAnimation.EasingFunction><PowerEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction></DoubleAnimation>
                              <DoubleAnimation Storyboard.TargetName="pinContentScale" Storyboard.TargetProperty="ScaleY" From="0.95" To="1" Duration="0:0:0.25"><DoubleAnimation.EasingFunction><PowerEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction></DoubleAnimation>
                          </Storyboard>
                          <Storyboard x:Key="SwitchPinToQrAnim">
                              <DoubleAnimation Storyboard.TargetName="pinContentTrans" Storyboard.TargetProperty="X" To="140" Duration="0:0:0.25"><DoubleAnimation.EasingFunction><PowerEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction></DoubleAnimation>
                              <DoubleAnimation Storyboard.TargetName="pinCodeContent" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2" />
                              <DoubleAnimation Storyboard.TargetName="qrContentTrans" Storyboard.TargetProperty="X" To="0" Duration="0:0:0.25"><DoubleAnimation.EasingFunction><PowerEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction></DoubleAnimation>
                              <DoubleAnimation Storyboard.TargetName="qrCodeContent" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.25" />
                              <DoubleAnimation Storyboard.TargetName="qrContentScale" Storyboard.TargetProperty="ScaleX" From="0.95" To="1" Duration="0:0:0.25"><DoubleAnimation.EasingFunction><PowerEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction></DoubleAnimation>
                              <DoubleAnimation Storyboard.TargetName="qrContentScale" Storyboard.TargetProperty="ScaleY" From="0.95" To="1" Duration="0:0:0.25"><DoubleAnimation.EasingFunction><PowerEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction></DoubleAnimation>
                          </Storyboard>
                      </Grid.Resources>
                      <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="MaxHeight" Value="352" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Visibility, ElementName=FileExplorer}" Value="Visible">
                                    <Setter Property="MaxHeight" Value="Infinity" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Visibility, ElementName=SettingsPanel}" Value="Visible">
                                    <Setter Property="MaxHeight" Value="Infinity" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    
                    <DockPanel x:Name="menuContentPanel" LastChildFill="True">
                        <DockPanel.RenderTransform>
                            <TranslateTransform x:Name="menuContentTrans" X="0" Y="0" />
                        </DockPanel.RenderTransform>
                    <StackPanel x:Name="TopActionsPanel" DockPanel.Dock="Top">
                        <StackPanel.Resources>
                            <Storyboard x:Key="ShowPinAnim">
                                <DoubleAnimation Storyboard.TargetName="btnToggleTopmost" Storyboard.TargetProperty="Width" To="16" Duration="0:0:0.2">
                                    <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetName="btnToggleTopmost" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2" />
                                <ThicknessAnimation Storyboard.TargetName="btnToggleTopmost" Storyboard.TargetProperty="Margin" To="0,0,8,0" Duration="0:0:0.2">
                                    <ThicknessAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></ThicknessAnimation.EasingFunction>
                                </ThicknessAnimation>
                            </Storyboard>
                            <Storyboard x:Key="HidePinAnim">
                                <DoubleAnimation Storyboard.TargetName="btnToggleTopmost" Storyboard.TargetProperty="Width" To="0" Duration="0:0:0.2">
                                    <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseIn"/></DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetName="btnToggleTopmost" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2" />
                                <ThicknessAnimation Storyboard.TargetName="btnToggleTopmost" Storyboard.TargetProperty="Margin" To="0,0,0,0" Duration="0:0:0.2">
                                    <ThicknessAnimation.EasingFunction><CubicEase EasingMode="EaseIn"/></ThicknessAnimation.EasingFunction>
                                </ThicknessAnimation>
                            </Storyboard>
                            <Storyboard x:Key="ShowAdbAnim">
                                <DoubleAnimation Storyboard.TargetName="pnlAdbStatus" Storyboard.TargetProperty="Height" To="39" Duration="0:0:0.25">
                                    <DoubleAnimation.EasingFunction><PowerEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetName="pnlAdbStatus" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.25" />
                            </Storyboard>
                            <Storyboard x:Key="HideAdbAnim">
                                <DoubleAnimation Storyboard.TargetName="pnlAdbStatus" Storyboard.TargetProperty="Height" To="0" Duration="0:0:0.2">
                                    <DoubleAnimation.EasingFunction><PowerEase EasingMode="EaseIn"/></DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetName="pnlAdbStatus" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2" />
                            </Storyboard>
                        </StackPanel.Resources>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,14">
                            <ToggleButton x:Name="btnToggleTopmost" Width="0" Height="16" Background="Transparent" BorderThickness="0" Margin="0,0,0,0" Opacity="0" ToolTip="Pin Location">
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border x:Name="bgBorder" Background="Transparent" CornerRadius="4">
                                                        <Grid>
                                                            <TextBlock x:Name="iconBase" Text="&#xE718;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                            <TextBlock x:Name="iconHover" Opacity="0" Text="&#xE718;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="11" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                            <TextBlock x:Name="iconChecked" Opacity="0" Text="&#xE718;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="11" Foreground="{DynamicResource SecondaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                        </Grid>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="bgBorder" Property="Background" Value="#1A808080" />
                                                            <Trigger.EnterActions>
                                                                <BeginStoryboard>
                                                                    <Storyboard>
                                                                        <DoubleAnimation Storyboard.TargetName="iconHover" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15" />
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </Trigger.EnterActions>
                                                            <Trigger.ExitActions>
                                                                <BeginStoryboard>
                                                                    <Storyboard>
                                                                        <DoubleAnimation Storyboard.TargetName="iconHover" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15" />
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </Trigger.ExitActions>
                                                        </Trigger>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Trigger.EnterActions>
                                                                <BeginStoryboard>
                                                                    <Storyboard>
                                                                        <DoubleAnimation Storyboard.TargetName="iconChecked" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2" />
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </Trigger.EnterActions>
                                                            <Trigger.ExitActions>
                                                                <BeginStoryboard>
                                                                    <Storyboard>
                                                                        <DoubleAnimation Storyboard.TargetName="iconChecked" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2" />
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </Trigger.ExitActions>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>
                            <Border x:Name="dragPill" Width="66" Height="16" Cursor="Hand" Background="Transparent" ToolTip="Drag to move. Double-click to reset.">
                                <Grid VerticalAlignment="Center">
                                    <Border Height="4" Background="{DynamicResource SecondaryTextBrush}" Opacity="0.4" CornerRadius="2" />
                                    <Border x:Name="dragPillAccent" Height="4" Background="{DynamicResource SecondaryBrush}" Opacity="0" CornerRadius="2" />
                                </Grid>
                            </Border>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,12">
                    <ToggleButton Name="btnQAConnect" Style="{StaticResource QuickActionBtn}" Margin="3,0" ToolTip="Connect ADB">
                        <TextBlock Text="&#xE71B;" />
                    </ToggleButton>
                    <ToggleButton Name="btnQAMirror" Style="{StaticResource QuickActionBtn}" Margin="3,0" ToolTip="Mirror Phone">
                        <TextBlock Text="&#xE8EA;" />
                    </ToggleButton>
                    <ToggleButton Name="btnQAPull" Style="{StaticResource QuickActionBtn}" Margin="3,0" ToolTip="Transfers">
                        <TextBlock Text="&#xE8B7;" />
                    </ToggleButton>
                    <ToggleButton Name="btnQAClipboard" Style="{StaticResource QuickActionBtn}" Margin="3,0" ToolTip="Clipboard">
                        <TextBlock Text="&#xE77F;" />
                    </ToggleButton>
                    <Button Name="btnCloseMenu" Style="{StaticResource DangerQuickActionBtn}" Margin="3,0" ToolTip="Close" Visibility="Collapsed" Opacity="0">
                        <TextBlock Text="&#xE711;" />
                    </Button>
                </StackPanel>
                
                <StackPanel Name="pnlAdbStatus" Height="0" Opacity="0" ClipToBounds="True">
                    <Separator Background="{DynamicResource AccentBrush}" Height="1" Margin="16,0" />
                    <StackPanel Orientation="Horizontal" Margin="24,10,24,10">
                        <TextBlock Name="txtStatus" Text="ADB Status: Initializing..." Foreground="{DynamicResource SecondaryTextBrush}" FontSize="13" FontFamily="Segoe UI" VerticalAlignment="Center" />
                        <Button Name="btnCopyIP" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource SecondaryTextBrush}" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="14" ToolTip="Copy IP:Port" Cursor="Hand" Visibility="Collapsed" VerticalAlignment="Center" Margin="6,0,0,0">
                            <TextBlock Text="&#xE8C8;" />
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" Background="Transparent" CornerRadius="4" Padding="4">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </StackPanel>
                <Separator Background="{DynamicResource AccentBrush}" Height="1" Margin="16,0" />
                    </StackPanel>
                
                    <!-- Your Devices Section -->
                    <ScrollViewer VerticalScrollBarVisibility="Hidden" PanningMode="VerticalOnly">
                        <StackPanel Margin="0,0,0,8">
                            <!-- Dynamic UDP Discovered Devices -->
                            <TextBlock Text="Discovered Devices" FontSize="13" Foreground="{DynamicResource SecondaryTextBrush}" FontWeight="SemiBold" Margin="12,12,0,4">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Items.Count, ElementName=icUdpPeers}" Value="0">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            
                            <ItemsControl x:Name="icUdpPeers">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button Style="{StaticResource SpatialListItem}" Margin="0,2,0,2" Tag="{Binding [Ip]}">
                                            <Button.ContextMenu>
                                                <ContextMenu Background="{DynamicResource AccentBrush}" Foreground="{DynamicResource PrimaryTextBrush}" BorderBrush="{DynamicResource AccentBrush}">
                                                    <MenuItem Name="menuPair" Header="Request PIN (Pair)" Tag="{Binding [Fingerprint]}" />
                                                    <MenuItem Name="menuGuestConnect" Header="Connect ADB" Tag="{Binding [Ip]}" />
                                                    <MenuItem Name="menuGuestCopyIp" Header="Copy IP Address" Tag="{Binding [Ip]}" />
                                                    <Separator Background="{DynamicResource PrimaryTextBrush}" />
                                                    <MenuItem Name="menuGuestForget" Header="Forget Device" Tag="{Binding [Fingerprint]}" Foreground="{DynamicResource DangerBrush}" />
                                                </ContextMenu>
                                            </Button.ContextMenu>
                                            <Button.Triggers>
                                                <EventTrigger RoutedEvent="Loaded">
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5" />
                                                            <DoubleAnimation Storyboard.TargetProperty="Height" From="0" To="64" Duration="0:0:0.3" >
                                                                <DoubleAnimation.EasingFunction><PowerEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </EventTrigger>
                                            </Button.Triggers>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                
                                                <Grid Width="38" Height="38" Margin="0,0,12,0">
                                                    <Ellipse Fill="{DynamicResource SecondaryBrush}" />
                                                    <TextBlock Text="&#xE8EA;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets, Segoe UI" Foreground="{DynamicResource SecondaryForegroundBrush}" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                    <Ellipse Width="12" Height="12" Fill="{DynamicResource SecondaryBrush}" Stroke="{DynamicResource AccentBrush}" StrokeThickness="2" HorizontalAlignment="Right" VerticalAlignment="Bottom" />
                                                </Grid>
                                                
                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding [Alias]}" FontSize="15" FontFamily="Segoe UI" FontWeight="Medium" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                                    <TextBlock Text="{Binding [DeviceModel]}" FontSize="13" Foreground="{DynamicResource SecondaryTextBrush}" />
                                                </StackPanel>
                                                
                                            </Grid>
                                        </Button>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            
                            <TextBlock Text="Your Devices" FontSize="13" Foreground="{DynamicResource SecondaryTextBrush}" FontWeight="SemiBold" Margin="12,12,0,4" />
                            
                <!-- User Joe (always visible when contracted) -->
                <Button x:Name="btnUserJoe" Style="{StaticResource SpatialListItem}" Margin="0,2,0,2">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        
                        <Grid Width="38" Height="38" Margin="0,0,12,0">
                            <Ellipse>
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="file:///$($PSScriptRoot -replace '\\', '/')/../Assets/JoeAvatar.jpg" Stretch="UniformToFill" AlignmentY="Top" />
                                </Ellipse.Fill>
                            </Ellipse>
                            <Ellipse Width="12" Height="12" Fill="{DynamicResource SecondaryBrush}" Stroke="{DynamicResource AccentBrush}" StrokeThickness="2" HorizontalAlignment="Right" VerticalAlignment="Bottom" />
                        </Grid>
                        
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="DeXStudios" FontSize="15" FontFamily="Segoe UI" FontWeight="Medium" Foreground="{DynamicResource PrimaryTextBrush}"/>
                            <TextBlock Text="dexify@dex.net" FontSize="13" Foreground="{DynamicResource SecondaryTextBrush}" />
                        </StackPanel>
                        
                        <TextBlock Grid.Column="2" Text="&#xE7F8;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0" />
                    </Grid>
                </Button>

                <!-- Device: Galaxy S21 (unhidden, original phone icon avatar) -->
                <RadioButton x:Name="btnDeviceGalaxy" Style="{DynamicResource SpatialDeviceItem}" Margin="0,2,0,2">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        
                        <Grid Width="38" Height="38" Margin="0,0,12,0">
                            <Ellipse Fill="{DynamicResource SecondaryBrush}" />
                            <TextBlock Text="&#xE8EA;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Foreground="{DynamicResource SecondaryForegroundBrush}" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Grid>
                        
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Galaxy S21" FontSize="15" FontFamily="Segoe UI" FontWeight="Medium" Foreground="{DynamicResource PrimaryTextBrush}"/>
                        </StackPanel>
                    </Grid>
                </RadioButton>

                <!-- Device: Windows (always visible when contracted) -->
                <RadioButton x:Name="btnDeviceWindows" Style="{DynamicResource SpatialDeviceItem}" Margin="0,2,0,2">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        
                        <Grid Width="38" Height="38" Margin="0,0,12,0">
                            <Ellipse Fill="{DynamicResource SecondaryBrush}" />
                            <TextBlock Text="&#xE7F8;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Foreground="{DynamicResource SecondaryForegroundBrush}" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Grid>
                        
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Windows" FontSize="15" FontFamily="Segoe UI" FontWeight="Medium" Foreground="{DynamicResource PrimaryTextBrush}"/>
                        </StackPanel>
                    </Grid>
                </RadioButton>


                <!-- Nearby Expand Panel: hidden when contracted, staggers in when expanded -->
                <StackPanel x:Name="NearbyExpandPanel" Visibility="Collapsed" Opacity="0">
                    <Separator Background="{DynamicResource AccentBrush}" Height="1" Margin="16,6" />

                    <!-- ====================================================================
                         Static "Nearby" profile placeholders (btnUser1/2/3).
                         INTENTIONAL SCAFFOLDING - DO NOT REMOVE. These render sample remote
                         profiles as visual scaffolding for the upcoming CROSS-EMAIL (WAN)
                         transfer feature. They have no handlers yet; they exist purely to
                         shape the WAN device-list layout until that feature is implemented,
                         at which point they will be replaced by real remote profiles or removed.
                         ==================================================================== -->

                    <!-- User 1 -->
                    <Button x:Name="btnUser1" Style="{StaticResource SpatialListItem}" Margin="0,2,0,2">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            
                            <Grid Width="38" Height="38" Margin="0,0,12,0">
                                <Ellipse>
                                    <Ellipse.Fill>
                                        <ImageBrush ImageSource="file:///$($PSScriptRoot -replace '\\', '/')/../Assets/User1Avatar.png" Stretch="UniformToFill" AlignmentY="Top" />
                                    </Ellipse.Fill>
                                </Ellipse>
                                <Ellipse Width="12" Height="12" Fill="{DynamicResource SecondaryBrush}" Stroke="{DynamicResource AccentBrush}" StrokeThickness="2" HorizontalAlignment="Right" VerticalAlignment="Bottom" />
                            </Grid>
                            
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Ama Serwaa" FontSize="15" FontFamily="Segoe UI" FontWeight="Medium" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                <TextBlock Text="user2@dex.net" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" />
                            </StackPanel>
                            
                            <!-- TODO: Detect device type to choose between phone (xE8EA) and laptop (xE7F8) icons for WAN profiles -->
                            <TextBlock Grid.Column="2" Text="&#xE8EA;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0" />
                        </Grid>
                    </Button>

                    <!-- User 2 -->
                    <Button x:Name="btnUser2" Style="{StaticResource SpatialListItem}" Margin="0,2,0,2">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            
                            <Grid Width="38" Height="38" Margin="0,0,12,0">
                                <Ellipse>
                                    <Ellipse.Fill>
                                        <ImageBrush ImageSource="file:///$($PSScriptRoot -replace '\\', '/')/../Assets/User2Avatar.png" Stretch="UniformToFill" AlignmentY="Top" />
                                    </Ellipse.Fill>
                                </Ellipse>
                                <Ellipse Width="12" Height="12" Fill="{DynamicResource SecondaryBrush}" Stroke="{DynamicResource AccentBrush}" StrokeThickness="2" HorizontalAlignment="Right" VerticalAlignment="Bottom" />
                            </Grid>
                            
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Akua Donkor" FontSize="15" FontFamily="Segoe UI" FontWeight="Medium" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                <TextBlock Text="user1@dex.net" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" />
                            </StackPanel>
                            
                            <!-- TODO: Detect device type to choose between phone (xE8EA) and laptop (xE7F8) icons for WAN profiles -->
                            <TextBlock Grid.Column="2" Text="&#xE7F8;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0" />
                        </Grid>
                    </Button>

                    <!-- User 3 -->
                    <Button x:Name="btnUser3" Style="{StaticResource SpatialListItem}" Margin="0,2,0,2">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            
                            <Grid Width="38" Height="38" Margin="0,0,12,0">
                                <Ellipse>
                                    <Ellipse.Fill>
                                        <ImageBrush ImageSource="file:///$($PSScriptRoot -replace '\\', '/')/../Assets/User3Avatar.jpg" Stretch="UniformToFill" AlignmentY="Top" />
                                    </Ellipse.Fill>
                                </Ellipse>
                                <Ellipse Width="12" Height="12" Fill="{DynamicResource SecondaryBrush}" Stroke="{DynamicResource AccentBrush}" StrokeThickness="2" HorizontalAlignment="Right" VerticalAlignment="Bottom" />
                            </Grid>
                            
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Kwame Asante" FontSize="15" FontFamily="Segoe UI" FontWeight="Medium" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                <TextBlock Text="user3@dex.net" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" />
                            </StackPanel>
                            
                            <!-- TODO: Detect device type to choose between phone (xE8EA) and laptop (xE7F8) icons for WAN profiles -->
                            <TextBlock Grid.Column="2" Text="&#xE8EA;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0" />
                        </Grid>
                    </Button>
                    
                    <!-- Dynamic Live Peers (Spawns indefinitely as mDNS peers connect) -->
                    <ItemsControl x:Name="icLivePeers">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <RadioButton Style="{DynamicResource SpatialDeviceItem}" Margin="0,2,0,2" Tag="{Binding [IP]}">
                                    <RadioButton.Triggers>
                                        <EventTrigger RoutedEvent="Loaded">
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4" />
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                    </RadioButton.Triggers>
                                    <RadioButton.ContextMenu>
                                        <ContextMenu Background="{DynamicResource AccentBrush}" Foreground="{DynamicResource PrimaryTextBrush}" BorderBrush="{DynamicResource AccentBrush}">
                                            <MenuItem Name="menuClipboard" Header="Send Clipboard" Tag="{Binding [IP]}" />
                                            <MenuItem Name="menuMirror" Header="Mirror Screen" Tag="{Binding [IP]}" />
                                            <MenuItem Name="menuCopyIp" Header="Copy IP Address" Tag="{Binding [IP]}" />
                                            <MenuItem Name="menuDisconnect" Header="Disconnect ADB" Tag="{Binding [IP]}" />
                                            <Separator Background="{DynamicResource PrimaryTextBrush}" />
                                            <MenuItem Name="menuRename" Header="Rename / Alias" Tag="{Binding [Fingerprint]}" />
                                            <MenuItem Name="menuForget" Header="Forget Device" Tag="{Binding [Fingerprint]}" Foreground="{DynamicResource DangerBrush}" />
                                        </ContextMenu>
                                    </RadioButton.ContextMenu>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        
                                        <Grid Width="38" Height="38" Margin="0,0,12,0">
                                            <Ellipse Fill="{DynamicResource SecondaryBrush}" />
                                            <TextBlock Text="{Binding [IconGlyph]}" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets, Segoe UI" Foreground="{DynamicResource SecondaryForegroundBrush}" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                            <Ellipse Width="12" Height="12" Fill="{DynamicResource SecondaryBrush}" Stroke="{DynamicResource AccentBrush}" StrokeThickness="2" HorizontalAlignment="Right" VerticalAlignment="Bottom" />
                                        </Grid>
                                        
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding [Name]}" FontSize="15" FontFamily="Segoe UI" FontWeight="Medium" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                            <TextBlock Text="{Binding [SubText]}" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets, Segoe UI" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" />
                                        </StackPanel>
                                    </Grid>
                                </RadioButton>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </DockPanel>
                    

                    <!-- PIN View -->
                    <Grid x:Name="pinViewPanel" Visibility="Collapsed" Opacity="0" Margin="16">
                        <Grid.RenderTransform>
                            <TranslateTransform x:Name="pinViewTrans" X="300" />
                        </Grid.RenderTransform>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                            <!-- Header / Status -->
                            <Grid Grid.Row="0">
                                <TextBlock x:Name="txtPinTitle" Grid.Row="0" Text="Pairing Request" FontSize="18" FontFamily="Segoe UI" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,8,0,0" HorizontalAlignment="Center" TextTrimming="CharacterEllipsis" MaxWidth="280" ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}" />
                                <TextBlock x:Name="txtPinSubtitle" Grid.Row="0" Text="from Galaxy S21" FontSize="14" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,34,0,0" HorizontalAlignment="Center" TextTrimming="CharacterEllipsis" MaxWidth="280" ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}" />
                            </Grid>
                        
                        <Grid Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <StackPanel x:Name="pinCodeContent" Visibility="Visible" RenderTransformOrigin="0.5,0.5">
                                <StackPanel.RenderTransform>
                                    <TransformGroup>
                                        <ScaleTransform x:Name="pinContentScale" ScaleX="1" ScaleY="1" />
                                        <TranslateTransform x:Name="pinContentTrans" X="140" />
                                    </TransformGroup>
                                </StackPanel.RenderTransform>
                                <ItemsControl x:Name="icPinDigits" Margin="0,10,0,0" HorizontalAlignment="Center">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="{DynamicResource AccentBrush}" CornerRadius="6" MinWidth="44" MinHeight="56" Margin="4,0" Padding="8,4">
                                                <TextBlock Text="{Binding}" FontSize="32" FontFamily="Segoe UI" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
                                    <TextBlock x:Name="txtPinStatus" Text="Waiting for acceptance..." FontSize="13" Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center" />
                                </StackPanel>
                                <TextBlock x:Name="txtPinTimeout" Text="" FontSize="13" Foreground="{DynamicResource SecondaryTextBrush}" HorizontalAlignment="Center" Margin="0,4,0,0" Opacity="0.7" />
                            </StackPanel>
                            <Border x:Name="qrCodeContent" Visibility="Collapsed" Background="White" CornerRadius="8" Padding="12" Margin="16,0" RenderTransformOrigin="0.5,0.5">
                                <Border.RenderTransform>
                                    <TransformGroup>
                                        <ScaleTransform x:Name="qrContentScale" ScaleX="1" ScaleY="1" />
                                        <TranslateTransform x:Name="qrContentTrans" X="0" />
                                    </TransformGroup>
                                </Border.RenderTransform>
                                <Image x:Name="imgQrCode" Width="140" Height="140" Stretch="Uniform" />
                            </Border>
                        </Grid>
                        
                        <Grid Grid.Row="2" Margin="0,0,0,8">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="8" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="8" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="8" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Button x:Name="btnPinCancel" Style="{StaticResource AnimatedActionBtn}" Grid.Row="0" Grid.Column="1" MinWidth="80" Content="Cancel" Foreground="{DynamicResource PrimaryTextBrush}" Background="{DynamicResource AccentBrush}" />
                            <Button x:Name="btnSettingsQrCode" Style="{StaticResource AnimatedActionBtn}" Grid.Row="0" Grid.Column="3" MinWidth="80" Foreground="Black" Background="{DynamicResource SecondaryBrush}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock x:Name="txtQrBtnIcon" Text="&#xED14;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" VerticalAlignment="Center" Margin="0,-2,6,0" />
                                    <TextBlock x:Name="txtQrBtnText" Text="QR CODE" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                            <Button x:Name="btnPinAccept" Style="{StaticResource AnimatedActionBtn}" Grid.Row="0" Grid.Column="5" MinWidth="80" Content="Accept" Foreground="{DynamicResource SecondaryForegroundBrush}" Background="{DynamicResource SecondaryBrush}" />
                            <Button x:Name="btnPinAcceptOnce" Style="{StaticResource AnimatedActionBtn}" Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="5" MinWidth="110" Content="Accept Once (Guest)" Foreground="{DynamicResource SecondaryTextBrush}" Background="Transparent" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1" Padding="0,8" FontSize="13" />
                        </Grid>
                    </Grid>
                </Grid>
                
                </DockPanel>
        </Border>
        </Border>
        

    </Grid>
    </Border>
</Window>

