/**
 * Canonical inline DeX wordmark: live text in the brand logo face
 * ('Creepy Notes', bold). Single source for contexts that interpolate raw
 * HTML (FAQ answers, step bodies); component contexts use BrandMark.astro,
 * which renders the same markup.
 */
export const brandWordmarkHtml = '<span class="font-logo font-bold">DeX</span>';
